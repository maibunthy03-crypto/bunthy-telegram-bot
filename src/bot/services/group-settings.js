const db=require('../../database/db');const env=require('../../config/env');
function getOrCreateGroup(chat){db.prepare(`INSERT INTO groups(chat_id,title,translation_enabled,primary_language,secondary_language,welcome_enabled) VALUES(?,?,?,?,?,?) ON CONFLICT(chat_id) DO UPDATE SET title=excluded.title,updated_at=CURRENT_TIMESTAMP`).run(String(chat.id),chat.title||'',env.translationEnabled?1:0,env.defaultPrimaryLanguage,env.defaultSecondaryLanguage,env.welcomeEnabled?1:0);return db.prepare('SELECT * FROM groups WHERE chat_id=?').get(String(chat.id))}
function updateGroup(chatId,fields){const allowed=['translation_enabled','primary_language','secondary_language','other_language_mode','welcome_enabled','welcome_message','staff_group_id'];const entries=Object.entries(fields).filter(([k])=>allowed.includes(k));if(!entries.length)return;const sql=`UPDATE groups SET ${entries.map(([k])=>`${k}=?`).join(',')},updated_at=CURRENT_TIMESTAMP WHERE chat_id=?`;db.prepare(sql).run(...entries.map(([,v])=>v),String(chatId))}
module.exports={getOrCreateGroup,updateGroup};
